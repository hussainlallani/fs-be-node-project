"use client";

// import { useState } from "react";
import NavBar from "./NavBar";

export default function NavBarWrapper() {
  //   const [searchText, setSearchText] = useState("");

  return (
    // <NavBar searchText={searchText} onSearch={(text) => setSearchText(text)} />
    <NavBar />
  );
}
